# FGO ダメージ・NP・スター計算機 for atwiki

atwiki の各サーヴァント個別ページへ、閉じた状態の計算機を自動設置する JavaScript です。

計算機では、次の結果を同時に表示します。

- ダメージ最小値・200通り平均・基準値・最大値
- NPリチャージ量
- スター獲得期待値
- スター獲得数の最小～最大

## ver.1.1.3 の主な機能

`FGO_DamageCalculator_atwiki.js` を共通メニューから1回読み込ませると、サーヴァント個別ページを自動判定します。

- 「保有スキル」の直前へ計算機を自動挿入
- 「保有スキル」がないページでは「性能」、さらにない場合は「プロフィール」の直前へ挿入
- 最初は折りたたんだ状態で表示
- 関係のないページには計算機を挿入しない
- ページの表示が遅れた場合も自動的に再検出

ページ内の基本情報表と隠しステータス表から、次の項目を自動入力します。

| 自動入力項目 | 読み取り元 |
| --- | --- |
| サーヴァント名 | 基本情報の「真名」 |
| 攻撃側クラス | 基本情報の「Class」 |
| ATK | レアリティに対応する自然上限LvのATK |
| N/A | 隠しステータスの「N/A」 |
| SR | 隠しステータスの「スター発生率」 |
| Quick・Arts・Buster・EX・宝具のHit数 | 隠しステータスの「ヒット数」 |
| 宝具カード種類 | 宝具表の「Card」 |
| 宝具倍率 | 最後に掲載された攻撃宝具表の宝具Lv.1倍率 |

ATKなどの自動入力後も、各入力欄は自由に編集できます。概念礼装、フォウ、クラススコアなどを加えた最終ATKへ変更して使用してください。

## 収録ファイル

| ファイル | 用途 |
| --- | --- |
| `FGO_DamageCalculator_atwiki.js` | 計算機本体。GitHubへ置くファイル |
| `atwiki_page_code.txt` | atwikiへ貼り付ける共通読み込みコード |
| `FGO_DamageCalculator_standalone.html` | パソコン上で表示確認するページ |
| `tests/test.js` | 計算式とページ情報解析のテスト |

## GitHubへの追加・更新方法

対象リポジトリは次の場所です。

```text
https://github.com/siroihuman/FGO_Calculator
```

ChatGPTから更新済みの場合は、GitHubのリポジトリを開き、`FGO_DamageCalculator_atwiki.js` の先頭付近が `const VERSION = "1.1.3";` になっていることを確認してください。

自分でブラウザから更新する場合は、次の手順です。

1. GitHubで `siroihuman/FGO_Calculator` を開きます。
2. `FGO_DamageCalculator_atwiki.js` を開きます。
3. 右上の鉛筆アイコン「Edit this file」を押します。
4. 新しいJavaScriptの内容へ置き換えます。
5. ページ下部の「Commit changes...」を押します。
6. 説明欄へ、たとえば `サーヴァントページへの自動設置に対応` と入力します。
7. もう一度「Commit changes」を押して確定します。

ファイルを新規アップロードする場合は、リポジトリ上部の「Add file」から「Upload files」を選びます。

## atwikiへの設置方法

### 重要：`include_js` は使用しません

この計算機は大きなJavaScriptであるため、`#include_js` では次のエラーになることがあります。

```text
include_js plugin Error : このプラグインで利用できない命令または文字列が入っています。
```

`#include_js(...)` は削除し、管理者用の `#javascript` プラグインで外部ファイルを読み込んでください。

また、`#javascript` の中へ外部の `<script>` タグを直接書く方式は使用しません。atwiki公式の利用例と同じ `document.write` を使い、計算機本体を読み込みます。

### おすすめ：共通メニューページへ1回だけ貼る

1. atwikiへ管理者としてログインします。
2. サイトの「メニュー」ページ（`https://w.atwiki.jp/siroi_human/pages/2.html`）を編集します。
3. 編集モードが「PukiWikiライクモード」であることを確認します。
4. ページの編集権限を「管理者のみ」にし、JavaScriptを置ける管理者用ページにします。
5. 古い `#include_js`、または `<script type="text/javascript" ...>` を含む古い `#javascript` ブロックがあれば、ブロック全体を削除します。
6. `atwiki_page_code.txt` の内容をメニュー本文の末尾へ貼り付けます。
7. 保存し、サーヴァント個別ページを開き直します。

```text
#javascript(){{
document.write("<span id='fgo-calculator-load-error' style='display:none;color:#b00020;font-weight:bold'>計算機ファイルを読み込めませんでした。</span>");
document.write("<script src='https://cdn.jsdelivr.net/gh/siroihuman/FGO_Calculator@main/FGO_DamageCalculator_atwiki.js?v=1.1.3'><\/script>");
setTimeout(function() {
  var fgoLoadError = document.getElementById("fgo-calculator-load-error");
  if (!window.FGODamageCalculator && fgoLoadError) {
    fgoLoadError.style.display = "block";
  }
}, 3000);
}}
```

メニューは各ページで読み込まれるため、全サーヴァントページを1ページずつ編集する必要はありません。JavaScriptは基本情報表を使ってサーヴァント個別ページを判定し、「保有スキル」の直前へ計算機を追加します。

### 個別ページだけで使用する場合

共通メニューへ置かず、特定のサーヴァントページの末尾へ `atwiki_page_code.txt` と同じコードを貼っても動作します。自動挿入される場所は、コードを貼った場所ではなく「保有スキル」の直前です。ただし、その個別ページも編集権限を「管理者のみ」にする必要があります。共通メニューと個別ページの両方へは貼らず、どちらか一方だけにしてください。

### 表示確認

サーヴァント個別ページを開くと、次のような閉じた見出しが表示されます。

```text
エインガナ：ダメージ・NP・スター計算機（ページ情報を自動入力）
```

見出しを押すと計算機が開きます。上部の「ページ情報」に、サーヴァント名、自然上限LvのATK、N/A、SRが表示されていれば自動取得成功です。

カード種類を変更すると、そのカードのHit数へ自動的に切り替わります。「攻撃種別」を「宝具」にすると、宝具のカード種類、Hit数、宝具Lv.1倍率へ切り替わります。

入力を変更した後、ページ情報へ戻したい場合は「ページ情報を再入力」を押してください。「入力を初期化」を押した場合も、そのサーヴァントの基本情報へ戻ります。

## 更新が見えない場合

GitHubの更新直後は、jsDelivrやブラウザのキャッシュにより古いJavaScriptが表示される場合があります。

1. ブラウザで強制再読み込みします。
2. まだ変わらない場合は、URL末尾のバージョン番号を変更します。

```text
変更前：?v=1.1.3
変更後：?v=1.1.4
```

番号はキャッシュを区別する目印です。JavaScript本体のバージョンと完全一致していなくても読み込みには影響しません。

## 表示されない場合の確認項目

1. GitHubリポジトリがPublicになっているか確認します。
2. リポジトリ名が `FGO_Calculator` になっているか確認します。
3. ファイル名の大文字・小文字が `FGO_DamageCalculator_atwiki.js` と一致しているか確認します。
4. `#include_js` が残っていないか確認します。
5. `#javascript(){{` と最後の `}}` が別々の行にあるか確認します。
6. JavaScriptを置いたatwikiページが、プラグインを使用できる管理者用ページか確認します。
7. 対象ページに「Class」「Rare」「ATK」「N/A」「スター発生率」「ヒット数」が載った表があるか確認します。

## パソコン上での表示確認

`FGO_DamageCalculator_standalone.html` と `FGO_DamageCalculator_atwiki.js` を同じフォルダーに置き、HTMLファイルをダブルクリックします。

単体確認ページにはサーヴァント表がないため、ページ情報の自動取得は行われず、初期値を手動入力する通常の計算機として表示されます。

## 計算時の注意

- ダメージは「1対象に対する全Hit合計」です。Hit数を掛ける必要はありません。
- NPとスターはHitごとに計算します。
- 攻撃対象数を2以上にすると、NPとスターだけが対象数分加算されます。
- オーバーキルHit数には、撃破判定が出たHit以降の数を入力します。
- 宝具表が強化前・強化後の順で複数ある場合は、最後の攻撃宝具表を現在値として読み取ります。
- 補助宝具など、攻撃倍率が掲載されていない宝具では宝具倍率を自動取得できません。
- ページの表記や構造がテンプレートと大きく異なる場合は、該当項目を手動入力してください。

## 入力内容の保存

入力内容はブラウザ内へ自動保存されます。保存単位は「同じ端末・同じブラウザ・同じatwikiページ」です。他の端末やブラウザとは共有されません。

## 参照資料

- [サーヴァント/隠しステータス - Fate/Grand Order @wiki](https://w.atwiki.jp/f_go/pages/304.html)
- [siroi_human - オリジナルサーヴァント＆エネミー](https://w.atwiki.jp/siroi_human/pages/54.html)

## 更新履歴

### ver.1.1.3

- atwiki公式の利用例と同じ`document.write`を使う外部ファイル読み込み方式へ変更
- 外部の計算機ファイルを読み込めなかった場合、赤字のエラーメッセージを表示

### ver.1.1.2

- atwiki側の読み込みコードを、外部の`<script>`タグを直接書く方式から、短いJavaScriptで計算機本体を読み込む方式へ変更
- `include_js`の安全判定を通さずに読み込める設置手順へ修正
- 共通メニューと個別ページのどちらでも同じ読み込みコードを使用可能

### ver.1.1.1

- 計算機の表示位置を「保有スキル」の直前へ変更
- 見出しがh2～h6のどの階層でも検出できるよう修正
- 隠しステータス表を認識できない場合も、基本情報表があれば計算機を表示
- ページ内容の読み込みが遅れた場合に自動検出を再試行

### ver.1.1.0

- サーヴァント個別ページへの折りたたみ自動挿入に対応
- ATK、クラス、N/A、SR、各カードHit数の自動入力に対応
- 宝具カード種類と宝具Lv.1倍率の自動入力に対応
- 強化前後の宝具表がある場合、後側の宝具表を採用
- ページ情報の再入力ボタンを追加
- `#include_js` を使わないatwiki設置手順へ修正

### ver.1.0.0

- 通常攻撃・宝具ダメージ計算を実装
- NPリチャージ計算を実装
- スター獲得期待値と最小～最大獲得量を実装
- 複数対象とHit単位のオーバーキル計算に対応
- クラス補正・クラス相性・DTDR・DSRの自動設定を実装
- 入力内容のブラウザ内自動保存とスマートフォン表示に対応
